$gitattrib = @'
# Auto detect text files and normalize to LF in the repo
* text=auto

# Force python scripts to use LF in repo (helps CI)
*.py text eol=lf
'@

Set-Content -Path .gitattributes -Value $gitattrib -Encoding UTF8
